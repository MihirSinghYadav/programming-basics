#include <fstream>
#include <iostream>
using namespace std;
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
int main()
{
    char ch;
    ifstream infile("TEXT.TXT");
    while (infile)
    {
        infile.get(ch);
        cout << ch;
    }
    cout << endl;
    return 0;
}