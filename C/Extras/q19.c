#include <stdio.h>
#include <math.h>
int main()
{
    int n, t;
    float playernumber = 1;
    scanf("%d", &t);
    while (t--)
    {
        scanf("%d", &n);
        if (n > 0.00001)
        {
        }
    }
}